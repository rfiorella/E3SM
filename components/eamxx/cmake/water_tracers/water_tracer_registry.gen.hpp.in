// AUTO-GENERATED by CMake from water_tracer_registry.gen.hpp.in
// DO NOT EDIT - configure via SCREAM_WATER_TRACERS_FILE

#ifndef WATER_TRACER_REGISTRY_GEN_HPP
#define WATER_TRACER_REGISTRY_GEN_HPP

#include <string_view>
#include <array>

namespace scream {
namespace WaterTracers {

// Catalog index mapping (from water_isotopes.hpp)
constexpr int catalog_index(const char* iso_name) {
  using namespace std::string_view_literals;
  constexpr std::string_view iso(iso_name);
  if (iso == "H2O"sv)    return 0;
  if (iso == "H216O"sv)  return 1;
  if (iso == "HDO"sv)    return 2;
  if (iso == "H218O"sv)  return 3;
  if (iso == "H217O"sv)  return 4;
  if (iso == "HTO"sv)    return 5;
  return -1; // Should never happen due to CMake validation
}

// Per-tracer metadata structure
struct WaterTracerInfo {
  std::string_view name;
  int catalog_idx;
  bool is_tag;
};

// Generated registry table
constexpr std::array<WaterTracerInfo, WTRC_MAX_CNST> WaterTracerRegistry = {{
@SCREAM_WATER_TRACER_REGISTRY_ENTRIES@
}};

} // namespace WaterTracers
} // namespace scream

#endif // WATER_TRACER_REGISTRY_GEN_HPP
